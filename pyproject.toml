[project]
name = "web-personal-cms-api"
version = "0.1.0"
description = "Public read-only JSON API for personal photo/video/post CMS (Cloudflare Python Worker)"
requires-python = ">=3.12"
dependencies = ["fastapi"]

[dependency-groups]
dev = ["workers-py", "workers-runtime-sdk"]
